## MODIFIED Requirements

### Requirement: 前一日事件采集(GDELT)
系统 SHALL 以 Google News RSS 为事件主通道,按五币种关键词组查询前一日窗口的文章列表;GDELT DOC 2.0 SHALL 降级为**空洞补位**通道——仅当某币种经主通道取得的条目数为 0 时才对该币种发起 GDELT 查询,未出现空洞的币种 MUST NOT 发起 GDELT 请求。

主通道 SHALL 在服务端时间窗之外**本地再做一次窗口过滤**:逐条按 RFC 2822 解析 `pubDate`,落在采集窗口外的条目 SHALL 排除,MUST NOT 依赖服务端时间参数——实测服务端过滤在部分查询形态下不生效。`pubDate` 无法解析的条目 SHALL 单独计数,MUST NOT 计为窗口内,也 MUST NOT 静默丢弃。

主通道 SHALL 以**域名白名单**作为相关性闸门:仅 `<source url=>` 主机名命中白名单的条目进入快照。白名单匹配 SHALL 为「完全相等或以点号加白名单项结尾」,MUST NOT 使用裸后缀匹配——后者会让 `notreuters.com` 命中 `reuters.com`。白名单 SHALL 存放于配置文件;配置缺失时该通道视为有意停用并回落 GDELT,MUST NOT 记为缺漏。

每币种的快照条目 SHALL 逐层记录过滤计数:源返回条数、`pubDate` 不可解析条数、窗口外条数、白名单外条数,并 SHALL 标注取数通道。源返回条数顶到通道上限时 SHALL 落盘截断标记。**缺少这些计数时,「该币种确实无事件」与「抓到大量条目但全部被过滤」在快照中不可分辨**,报告层会把管道状态叙述成市场事实。

条目的文章列表 SHALL 用**空值表示「没采到」、空列表表示「采到了、可用的 0 条」**,二者 MUST NOT 混用。混用会让两条通道同时失败的日子在跨日聚合中被计为「已采集」,进而被断言成「区间内确实 0 条,全区间采集完整、无截断」——彻底的管道停摆被渲染成市场事实。同理,源返回条数未知时截断标记 SHALL 为空值而非 false:**不知道有没有截断**与**知道没有截断**是两件事。

跨日聚合的「确实 0 条」SHALL 只在**看到过的每一条都能放进区间**时才允许:任何一条被逐层滤除、落在区间外、或时间戳不可解析的条目,都使区间内条数成为下界,结论 SHALL 退化为「有无事件无法判定」并给出对应计数。

该判定 SHALL 陈述在聚合器**自身的窗口账**上(看到几条、其中几条落进区间),MUST NOT 陈述在某一条通道的过滤账上——后者在不接主通道的日子结构上无法生效,且滤除量的判据一旦写成「当日一条都没留下」,留下过条目的日子(真实数据里的绝大多数)整层滤除量就退出了结论判定。滤除条次 SHALL 按「源返回条数 − 留存条数」逐日累计,MUST NOT 以当日是否归零为前提。

面向读者的结论句 MUST NOT 出现程序空值的字面量——区间内上限不唯一时改用不含数值的措辞;当日最终仍取得条目时 MUST NOT 措辞为「无一可用」,该说法与同句的「区间内至少 N 条」自相矛盾。

#### Scenario: 部分滤除仍须进入结论
- **WHEN** 某日主通道抓到 34 条、滤剩 5 条(留存数大于 0)
- **THEN** 被滤除的 29 条次仍计入结论判定并在结论句中给出,MUST NOT 因当日留下过条目而退出

#### Scenario: 采到的条目全部发布于区间外
- **WHEN** 某币种在区间内采到条目,但其发布日全部落在聚合区间之外(含不接主通道、快照中没有任何过滤账的形态)
- **THEN** 结论 MUST NOT 为「确实 0 条」,SHALL 说明采到的条数全部发布于区间外

主通道的截断标记 SHALL 记录在主通道自身的账内,补位通道覆写条目级字段时 MUST NOT 抹掉它。

「是否取得可署名来源的报道」SHALL 由采集层算成一个三态标志落盘,报告层只引用它,MUST NOT 由报告层自行组合「滤除计数为 0」与「文章列表为空」两个条件——补位成功的当日两个条件会给出相互矛盾的指引。该标志 SHALL 陈述在文章列表自身的定义域上,与其三态一一对应:列表为空值(没采到)时 SHALL 为空值,列表为空表(采到了、可用 0 条)时为真,列表非空时为假。把它挂在主通道的过滤账上会让**主通道有意停用**这一健康形态恒为空值——通道未启用而补位正常取回条目时,「取得了可署名来源」是已知事实,MUST NOT 记为不可知。

「源返回的原始样本触顶」与「落盘条数被上限钉住」SHALL 为两个独立字段,MUST NOT 合并为一个布尔。两者在不过滤的通道上恒等、在带过滤闸门的通道上不等:主通道滤除前的 100 条顶到 99,而落盘的是滤后的 11 条,离上限差 88。合并会让「已达当日采集上限、实际篇数只多不少」与「两日均达上限、差值 0 是上限造成的」这两条固定话术施加在一个远未触顶的数上,把真实的事件面持平渲染成上限假象。跨日聚合的结论句同理:引用哪个标记,就 SHALL 配用哪个标记自己的上限值,且同一天的同一次截断 MUST NOT 被两个计数各记一遍。

源返回但**结构不可识别**而被跳过的元素数 SHALL 落盘并参与跨日聚合的结论判定。缺少它时,「源改版后返回的元素全部无法识别」与「源确实一条都没有」在快照中完全同形(文章列表为空表、去重前条数非零、无任何缺漏),聚合器据此断言「区间内确实 0 条、全区间采集完整」。落盘后仍不可识别的元素同理 SHALL 单列,MUST NOT 计入「去重掉的重复条数」——把丢弃量伪装成重复量会让报告写出与事实相反的去重叙述。

#### Scenario: 源返回的元素结构不可识别
- **WHEN** 事件源改版,返回的列表元素不是对象
- **THEN** 被跳过的元素数落盘,跨日聚合的结论 MUST NOT 为「确实 0 条」

#### Scenario: 同一次截断不得记两遍
- **WHEN** 某日条目取自带过滤闸门的主通道,且其滤除前样本触顶
- **THEN** 「落盘条数被钉住」为假、「原始样本触顶」为真且只计一次,结论句只出现一次截断披露

#### Scenario: 主通道未启用时可署名来源标志不为空值
- **WHEN** 主通道未配置(有意停用)而补位通道正常取得条目
- **THEN** 「可署名来源缺席」为假而非空值,该币种不记为缺漏

#### Scenario: 两条通道都没跑成时可署名来源标志为空值
- **WHEN** 某币种当日主通道与补位通道均未取得条目
- **THEN** 「可署名来源缺席」为空值,报告层 MUST NOT 据此陈述「未取得可署名来源」或「该国无新闻」

#### Scenario: 主通道截断不得污染条数触顶标志
- **WHEN** 主通道返回条数顶到其上限后被全部滤除,补位通道取得远低于自身上限的条目
- **THEN** 「落盘条数被钉住」为假、「原始样本触顶」为真,结论句引用主通道上限值而非补位通道上限值

跨日派生量 SHALL 在前后两日取数通道不同时拒绝给出差值(记为空值)并标注前一日通道:两条通道的条数上限与筛选口径都不同,相减得到的差值不表示事件面变化。本变更之前生成的、不含通道标注的存量条目 SHALL 视为补位通道所产,MUST NOT 视为「未知因而与当日相同」。

#### Scenario: 两条通道都没采到时不得计为已采集
- **WHEN** 某币种主通道与补位通道当日均未取得条目
- **THEN** 该币种的文章列表为空值而非空列表,跨日聚合据此判定为「未采到」,结论 MUST NOT 出现「确实 0 条」

#### Scenario: 整区间被相关性闸门滤空
- **WHEN** 某币种在整个聚合区间内每天都抓到条目,但全部落在白名单之外
- **THEN** 结论为「未取得可署名来源、有无事件无法判定」并给出滤除总条数,MUST NOT 为「确实 0 条」

#### Scenario: 区间内上限不唯一
- **WHEN** 聚合区间内不同日期的条目来自上限不同的两条通道
- **THEN** 结论句改用不含数值的措辞说明上限不唯一,MUST NOT 把程序空值的字面量拼进句子

#### Scenario: 补位不得抹掉主通道的截断事实
- **WHEN** 主通道返回条数顶到其上限但全部被滤除,补位通道随后取得少量条目
- **THEN** 主通道的截断标记仍可从快照读出

#### Scenario: 可署名来源缺席由脚本判定
- **WHEN** 主通道滤除后为 0 条
- **THEN** 采集层落盘的「可署名来源缺席」布尔仅在补位也未取得条目时为真;补位成功时为假

#### Scenario: 跨通道不给差值
- **WHEN** 某币种前后两日的事件取自不同通道(含前一日条目不带通道标注的存量快照)
- **THEN** 事件数差值记为空值并标注前一日通道,报告 MUST NOT 据此陈述事件面变化

条目的 `url` 为跳转链而非原文直链时,SHALL 由通道标注使下游可辨,可回溯的来源归属由 `domain` 承担。

两条通道产出的条目 SHALL 使用**同一种时间戳格式**,使下游只需一个解析器。主通道条目的时间戳语义为**发布时间**、GDELT 条目为采见时间,二者的区别 SHALL 由通道字段承载,MUST NOT 通过改变格式来表达——格式分叉会让既有解析器把主通道的每一条都判成"时间戳不可解析",周度聚合器据此把整周降级为"无法判定"。

#### Scenario: 时间戳格式跨通道一致
- **WHEN** 主通道条目落盘
- **THEN** 其时间戳采用与 GDELT 条目相同的格式,既有的跨日聚合解析器无需改动即可解析

GDELT 补位查询 SHALL 串行发出,**请求间隔 SHALL ≥ 5 秒,默认 20 秒**;主通道无此约束(每币种 1 次请求)。补位查询 MUST 保留既有的限流处理:系统 MUST 识别"HTTP 200 但正文为限速提示"的软失败形态**与 HTTP 429 硬限流**,退避后重试一次,仍失败则记为缺漏。查询顺序 SHALL 按采集日期确定性轮转,使限流造成的缺失不恒定落在同一批币种(公平性措施;实测表明本机限流与查询位置无关,轮转不构成 429 缓解手段)。同一币种内标题重复的文章 SHALL 只保留一条。快照 MUST NOT 包含 tone 字段——所使用的 artlist 端点不返回该字段。

#### Scenario: 正常采集
- **WHEN** 五组关键词经主通道查询完成且各有白名单内条目
- **THEN** 快照含每币种的前一日文章列表(标题/URL/来源域名/时间)与通道标注,不含 tone 字段,且不发起任何 GDELT 请求

#### Scenario: 主通道条目被相关性闸门滤除
- **WHEN** 某币种主通道返回大量条目,但其中只有少数命中域名白名单
- **THEN** 快照仅含命中白名单的条目,并记录被白名单滤除的条数,使「源无数据」与「源有数据但不相关」可分辨

#### Scenario: 相关性闸门滤空后 GDELT 补位
- **WHEN** 某币种经主通道过滤后剩余 0 条
- **THEN** 系统对该币种发起 GDELT 查询;取得条目则落盘并标注通道为 GDELT,仍无所得则该币种事件记为缺漏

#### Scenario: 两条通道都无所得
- **WHEN** 某币种主通道过滤后为 0 条且 GDELT 补位亦失败
- **THEN** 该币种事件记为缺漏,缺漏原因 SHALL 体现两条通道均已尝试,其余币种不受影响

#### Scenario: 白名单匹配不得误命中相似域名
- **WHEN** 条目来源域名是白名单项的**后缀相似域名**(如白名单含 `reuters.com`,条目来自 `notreuters.com`)
- **THEN** 该条目 MUST NOT 命中白名单

#### Scenario: 白名单收录子域
- **WHEN** 条目来源域名是白名单项的子域(如白名单含 `philstar.com`,条目来自 `interaksyon.philstar.com`)
- **THEN** 该条目命中白名单

#### Scenario: 服务端时间窗不可信
- **WHEN** 主通道响应中含 `pubDate` 落在采集窗口之外的条目
- **THEN** 这些条目被排除并单独计数,MUST NOT 因服务端已声明时间窗而放行

#### Scenario: 发布时间不可解析
- **WHEN** 某条目的 `pubDate` 无法按 RFC 2822 解析
- **THEN** 该条目不计入窗口内、不进入快照,并单独计数落盘

#### Scenario: 源返回条数顶到上限
- **WHEN** 主通道某币种返回的条数等于该通道的条数上限
- **THEN** 快照标注该币种的取数被截断,使下游知道白名单内条数是从截断样本中得出的下界

#### Scenario: 主通道未配置
- **WHEN** 配置中没有主通道端点或白名单文件
- **THEN** 系统静默回落 GDELT(与既有「未配置 = 有意停用」约定一致),MUST NOT 记为缺漏

#### Scenario: 主通道响应畸形
- **WHEN** 主通道返回非 XML、空正文或结构不符的响应
- **THEN** 记入缺漏并让该币种走 GDELT 补位,采集层 MUST NOT 向上抛出异常

#### Scenario: 限速软失败退避
- **WHEN** GDELT 补位请求的响应为 HTTP 200 但正文是限速提示文本
- **THEN** 系统识别为软失败,等待后重试一次;重试成功则正常记录,再失败则该币种事件记为缺漏

#### Scenario: 硬限流退避
- **WHEN** GDELT 补位请求返回 HTTP 429
- **THEN** 系统等待后重试一次;重试成功则正常记录,再失败则该币种事件记为缺漏

#### Scenario: 查询顺序轮转
- **WHEN** 以不同采集日期运行且存在多个币种需要 GDELT 补位
- **THEN** 补位查询顺序按日期确定性轮转;同一日期重复运行顺序一致

#### Scenario: 标题去重
- **WHEN** 某币种返回的文章中存在标题完全相同的多条
- **THEN** 快照中该币种只保留其中一条

#### Scenario: 端点不可用
- **WHEN** GDELT 补位请求超时或返回错误
- **THEN** 该币种事件记为缺漏(含原因),其余币种查询继续,管线不中断
